import { z } from "zod"

export const completeSaleSchema = z.object({ organizationId: z.string().min(1), locationId: z.string().min(1), orderNumber: z.string().optional(), customerId: z.string().optional(), items: z.array(z.object({ variantId: z.string().min(1), qty: z.number().int().positive(), discountCents: z.number().int().nonnegative(), fulfillmentType: z.enum(["CASH_AND_CARRY", "PICKUP", "DELIVERY"]).default("CASH_AND_CARRY") })).min(1, "Add at least one item."), paymentMethod: z.enum(["CASH", "EFT"]), amountPaidCents: z.number().int().positive("Enter the payment amount."), approvalCode: z.string().optional() })
